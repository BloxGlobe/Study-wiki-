export default function Login(container) {
	container.innerHTML = `
		<section class="section">
			<h2>Login</h2>
			<p class="placeholder">Use the login modal (click the avatar or New Note).</p>
		</section>
	`;
}
