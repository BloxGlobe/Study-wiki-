export default function Signup(container) {
	container.innerHTML = `
		<section class="section">
			<h2>Sign up</h2>
			<p class="placeholder">Use the sign-up modal (click the avatar or New Note).</p>
		</section>
	`;
}
