export default function Projects(container) {
	container.innerHTML = `
		<section class="section">
			<h2>Projects</h2>
			<p class="placeholder">No projects yet. Start by creating one!</p>
		</section>
	`;
}
